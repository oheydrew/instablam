<h1>Insta<strong>Blam!</strong></h1>
<hr>

<div class="d-flex justify-content-between flex-wrap">
    <% @posts.each do |post| %>
        <div class="d-block p-2">
            <%= link_to image_tag(post.image_data, height: 175, class: 'img rounded border'), post_path(post[:id]) %>
        </div>
    <% end %>
</div>


